import './Layout.css'

function Header() {
    return(
    <header> 
        <span class="head" id="LogoSite">Logo / </span>
        <span class="head" id="PageName">Khotek</span>
    </header>
    );
}

export default Header;